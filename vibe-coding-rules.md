# 小狗语言翻译器 Vibe Coding Rules

## 1. 规则用途

本文件用于约束每一轮 Vibe Coding。开始编码前，编程助手必须先阅读：

1. `puppy-translator-plan.md`
2. `vibe-coding-plan.md`
3. `vibe-coding-rules.md`
4. 当前轮次涉及的现有代码和测试

发生冲突时，优先级如下：

1. 用户最新明确要求。
2. 本规则中的产品不可变规则和安全规则。
3. `puppy-translator-plan.md`。
4. `vibe-coding-plan.md` 当前轮次。
5. 现有代码习惯与实现细节。

## 2. 产品不可变规则

以下规则任何轮次都不得擅自修改：

- 这是微信原生小程序，不改成网页、React、Vue、Taro 或 uni-app。
- 录音必须使用微信真实录音能力，不能用假音频代替正式功能。
- 翻译文字来自审核后的本地预设文案库。
- 文案抽取与音频内容、音量、频率、时长和文件摘要无关。
- 小程序运行时不得调用文本生成模型或声音识别模型。
- 用户录音不得发送给 AI，也不得用于训练模型。
- 界面不得展示文案生成机制说明。
- 界面不得展示准确率、可信度、AI 识别、医疗诊断或健康建议。
- 左滑用于删除当前结果和相关临时文件。
- 右滑用于保存音频、便签和可选照片。
- 未经用户右滑确认保存的录音不得上传到云端。
- 照片必须可选；拒绝相机或相册权限时仍可保存音频和便签。
- 历史记录必须支持查看便签、查看照片、播放音频和删除记录。

## 3. 技术栈规则

- 小程序端使用微信原生框架、TypeScript、WXML、WXSS 和 JSON。
- 录音使用 `wx.getRecorderManager()`。
- 拍照或选图使用 `wx.chooseMedia()`；只有明确需要实时取景时才使用 `camera` 组件。
- 本地任务和缓存使用微信 Storage API。
- 云端优先使用微信云开发 CloudBase、云数据库、云存储和云函数。
- 云函数使用 Node.js 和 TypeScript，并遵循仓库已经建立的运行时版本。
- 不增加 Python、FastAPI、PyTorch、ONNX、音频分析或模型推理服务。
- 优先使用微信官方 API 和当前项目已有依赖，不为简单功能引入大型框架。
- 依赖版本必须写入 `package.json` 和 lockfile，禁止依赖未锁定的全局包。

## 4. 分轮开发规则

- 一轮只实现 `vibe-coding-plan.md` 中一个明确轮次。
- 不提前实现后续轮次，不顺手扩展未要求的功能。
- 开始修改前先说明本轮目标、预计修改文件和最小验证方式。
- 优先修改直接控制当前行为的模块，不进行无关重构。
- 保持现有公开类型、组件接口和数据结构，除非当前轮次明确要求变更。
- 新功能先完成最小可运行版本，再补本轮要求的边界处理和测试。
- 每轮结束时项目必须保持可编译、可运行、可回退。
- 当前轮次未完成验收前，不进入下一轮。
- 发现无关问题时记录但不修复，除非它阻止本轮功能完成。

## 5. 文件修改规则

- 修改前先阅读目标文件及其最近的调用方、类型或测试。
- 只修改完成本轮目标所需的文件。
- 不删除或覆盖用户已有改动。
- 不进行整仓格式化，不改变无关代码的缩进、命名和文件顺序。
- 新文件必须放入计划约定的目录，不在项目根目录随意堆放业务文件。
- 组件、服务、类型和文案数据分开存放，不把所有逻辑写进页面文件。
- 不复制相同业务逻辑到多个页面；已有公共能力应复用现有封装。
- 不提交构建输出、依赖目录、临时录音、真实照片、密钥或本地私密配置。
- 不擅自创建 Git 分支或提交；完成后只提供建议的提交信息，除非用户明确要求提交。

## 6. 录音规则

- 录音交互默认采用点击开始、再次点击结束。
- 单次录音最短 1 秒、最长 15 秒；参数变化必须先更新计划。
- 必须处理开始、停止、错误、自动停止、页面卸载和重复点击。
- 同一时刻只能存在一个录音任务和一个录音计时器。
- 用户离开页面时必须停止录音并释放计时器和监听器。
- 麦克风授权应在用户主动录音时申请，不在启动小程序时提前申请。
- 权限拒绝后提供清晰的恢复路径，不循环弹出授权请求。
- 录音过短时提示重新录制，不生成便签。
- 静音或环境噪声不阻止文案抽取，因为产品不分析声音内容。
- 临时音频路径和时长必须绑定到唯一任务 ID，避免多次录音串数据。

## 7. 状态管理规则

录音主流程使用明确状态，不使用多个互相矛盾的布尔值拼接状态。

允许的核心状态：

```text
idle -> recording -> generating -> pending -> saved
                    |              |
                    v              v
                  failed         failed
```

- `recording`：真实录音进行中。
- `generating`：本地趣味动画和文案抽取进行中。
- `pending`：便签已生成，等待用户左滑或右滑。
- `saved`：用户已确认保存，数据已持久化或进入可靠重试队列。
- `failed`：当前操作失败，可以重试或删除。
- 状态转换必须集中管理并可测试。
- `generating` 不是音频分析，不得命名为 `analyzing`。
- 状态变化期间禁止重复触发同一操作。
- 页面卸载时必须清理动画、计时器和未使用监听器。

## 8. 文案库规则

正式文案结构至少包含：

```ts
type PuppyMessage = {
  id: string;
  category: PuppyMessageCategory;
  text: string;
  enabled: boolean;
};
```

- 正式文案存放在独立、只读的 TypeScript 或 JSON 文件中。
- 第一版保留 80～150 条经过人工审核的正式文案。
- 分类限定为计划定义的枚举，不接受运行时任意字符串。
- 每条正文不超过 40 个汉字，不包含换行、链接或表情符号。
- ID 必须稳定且唯一；发布后不得把同一 ID 改成完全不同的内容。
- 文案不得包含医疗、疾病、受伤、攻击、恐吓、歧视、成人或危险行为内容。
- 文案不得输出准确率、可信度、真实情绪判断或健康建议。
- 页面组件不得硬编码正式文案，必须通过文案服务或生成器读取。
- 每次更新文案库必须提升 `generatorVersion` 并通过文案校验测试。

## 9. AI 生成候选文案规则

AI 只用于开发和内容维护阶段生成候选，不属于小程序运行架构。

- 先定义分类、风格、数量、长度和禁止内容，再让 AI 生成。
- 一次生成 200～300 条候选，最终人工筛选 80～150 条。
- AI 输出必须是约定结构的 JSON 数组，不接受混合说明文字。
- 原始候选存入开发草稿目录，不得被小程序直接 import。
- 候选必须先通过格式、ID、分类、长度和重复检查。
- 自动检查通过不等于正式可用，所有内容仍需人工逐条审核。
- 未经人工审核的候选不得进入正式文案库或发布包。
- 不把用户录音、照片、`openid`、云文件 ID 或其他用户数据提供给 AI。
- 不把 AppSecret、云密钥、上传私钥或生产凭据提供给 AI。
- 后续扩充必须重复“生成候选、自动校验、人工审核、版本化导入”流程。

## 10. 随机生成器规则

- 生成器只读取启用状态的正式本地文案。
- 生成器不得读取音频文件、音频元数据或任何声音特征。
- 最近 5 条文案 ID 应优先排除，降低连续重复概率。
- 候选不足时必须安全重置最近记录，禁止无限循环。
- 文案库只有 1 条时仍应返回该文案。
- 文案库为空时返回明确的内部错误，不生成空便签。
- 随机数函数应可注入，使单元测试能够得到确定结果。
- 最近结果可以存入 Storage，但 Storage 损坏不能导致页面崩溃。
- 文案生成必须离线可用，不得依赖网络、云函数或远程配置成功。

## 11. 界面与交互规则

- 第一屏直接提供录音主操作，不制作营销落地页。
- 录音按钮、状态文字和结果便签必须是页面的视觉重点。
- 便签只显示趣味分类、正文、录音时长和生成时间。
- 不在界面显示生成机制、准确率、可信度或医疗判断。
- “翻译中”动画固定约 1～2 秒，本地运行，不等待网络。
- 动画不得使用“AI 分析”“频谱识别”或类似真实分析表述。
- 动画和动态文字不得造成页面跳动、按钮位移或内容遮挡。
- 小屏幕、长文案和系统大字体下不得横向溢出。
- 触控区域必须足够大，禁用状态必须有明确视觉反馈。
- 图标优先使用项目已有图标库，不手绘不一致的图标。
- 不加入社区、排行榜、会员、营销页或与核心流程无关的装饰功能。

## 12. 滑动规则

- 左右滑动必须封装为可复用的 `swipe-item` 组件。
- 横向移动明显大于纵向移动时才锁定横向手势。
- 未达到触发阈值时自动回弹，不执行操作。
- 左滑只执行删除，右滑只执行保存，不允许方向含义互换。
- 删除后提供 3 秒本地撤销入口。
- 保存、删除或生成进行中时禁止重复滑动操作。
- 纵向滚动列表时不得频繁误触左右操作。
- 手势距离、方向和阈值计算应提取为纯函数并进行边界测试。

## 13. 照片与音频播放规则

- 右滑保存后再询问拍照、从相册选择或跳过。
- 拒绝照片权限不得阻止无照片保存。
- 图片必须限制格式和大小，并处理选择取消和加载失败。
- 详情页支持音频播放、暂停和进度显示。
- 离开详情页时必须停止播放并释放音频实例。
- 同一时刻只允许一条历史音频播放。
- 删除历史记录时必须同时清理其音频和可选照片。

## 14. 本地保存与任务恢复规则

- 在云端功能完成前，本地版本必须能保存记录和媒体。
- 临时文件需要转存到持久化路径后才能作为本地历史记录使用。
- Storage 只保存必要元数据，不把二进制媒体编码后直接塞入 Storage。
- 云端启用后，Storage 只作为缓存和待同步任务队列，不作为最终事实来源。
- 每个待处理任务必须包含任务 ID、幂等键、阶段、重试次数和更新时间。
- 区分可重试错误与永久错误，不进行无限自动重试。
- 任务恢复不得产生重复记录或重复媒体文件。
- 超过保留期限的失败任务应允许用户删除或重新录制。

## 15. 云端与数据规则

- 只有用户右滑确认保存后才能上传音频和可选照片。
- 云端只保存媒体和便签，不分析音频内容。
- 所有云函数从服务端上下文取得 `openid`。
- 客户端提供的 `openid` 不可信，云函数不得以其作为数据归属依据。
- 所有读、写、删除操作必须校验当前用户的数据所有权。
- 云函数输入必须进行结构化校验，拒绝未知字段和非法状态。
- 保存操作必须使用幂等键，防止重复点击产生重复记录。
- 服务端使用 `messageId` 和 `generatorVersion` 校验正式文案，不信任客户端任意正文。
- 历史记录按时间倒序分页，分页不得重复或遗漏。
- 删除记录时同时处理数据库、音频和照片；部分失败必须可重试。
- 超过 24 小时且未保存的临时文件由定时任务清理。
- 正式记录的媒体不得被临时清理任务误删。

## 16. 隐私与安全规则

- 只在用户主动操作时申请麦克风、相机或相册权限。
- 明确说明麦克风用于录音，相机或相册用于关联可选照片。
- 不进行后台监听，不采集与核心功能无关的数据。
- API 和媒体传输使用微信云能力或 HTTPS。
- 日志不得记录完整音频、照片、`openid`、密钥或不必要的用户数据。
- 环境 ID、AppID 和非敏感配置集中管理；密钥只存放于安全环境变量或平台密钥管理。
- 禁止在代码、文档、测试快照和日志中写入真实密钥。
- 数据库和云存储遵循最小权限原则。
- 用户必须能够删除单条记录和清除自己的全部内容。
- 不提供医疗诊断或健康建议，不增加相关字段和业务分支。

## 17. 测试规则

每一轮至少执行与改动范围最接近的验证，不允许只凭阅读代码判断完成。

- TypeScript 改动运行类型检查。
- 样式和模板改动在微信开发者工具中编译并检查目标页面。
- 录音、相机、文件和音频播放必须使用 iOS 或 Android 真机验证。
- 纯函数和状态机必须增加或更新单元测试。
- 云函数必须测试身份隔离、输入校验、幂等和删除权限。
- 随机生成器必须测试防重复、单条文案、空候选和 Storage 损坏。
- 文案库必须测试 ID 唯一、正文唯一、分类合法、正文非空和长度上限。
- 修复测试失败时只处理本轮引入的问题，不顺手修复无关失败。
- 测试失败不得宣称本轮完成；无法执行的测试必须说明原因和剩余风险。

发布前必须覆盖：

- 麦克风和相机权限允许、拒绝及重新授权。
- 超短录音、15 秒自动停止、快速重复点击和页面切换。
- 静音、人的声音和背景噪声下仍能生成趣味便签。
- 左右滑动、纵向滚动误触和删除撤销。
- 有照片、无照片、照片加载失败和音频播放。
- 弱网、断网、上传中断、任务恢复和重复保存。
- iOS 与 Android 微信客户端的完整流程。

## 18. 每轮完成标准

只有同时满足以下条件，当前轮次才算完成：

- 本轮验收标准全部满足。
- 没有提前实现后续轮次。
- 代码通过可用的格式检查、类型检查和测试。
- 微信开发者工具可以编译目标页面。
- 涉及设备能力时已完成真机验证，或明确记录未验证原因。
- 没有新增真实密钥、调试后门、无主临时文件或未说明的 mock。
- 新增配置、权限、数据库字段和云资源均有文档说明。
- 修改范围与本轮目标一致，没有无关重构。
- 编程助手给出修改文件、验证结果、遗留问题和建议提交信息。

## 19. 编程助手输出规则

每轮结束时，编程助手按以下顺序汇报：

1. 本轮完成内容。
2. 修改文件及各自用途。
3. 执行的检查、测试和结果。
4. 需要用户在微信开发者工具或 CloudBase 手工完成的配置。
5. 明确保留到后续轮次的内容。
6. 当前已知风险或未验证项。
7. 建议的 Git 提交信息。

不得只回复“已完成”，也不得声称运行了实际没有运行的测试。

## 20. 每轮通用提示词

```text
请先阅读 puppy-translator-plan.md、vibe-coding-plan.md、vibe-coding-rules.md 和当前代码。
严格执行 vibe-coding-rules.md，只实现当前指定轮次，不提前开发后续功能，不修改无关文件。

开始前先说明：
1. 本轮目标；
2. 预计修改的文件；
3. 能够否定当前实现方案的最小验证方式。

实现后运行当前项目可用的格式检查、类型检查、测试和构建。涉及录音、相机、文件或音频播放时，列出必须在微信真机验证的步骤。

最后汇报：完成内容、修改文件、验证结果、手工配置、遗留问题、后续轮次边界和建议的 Git 提交信息。不要提交 Git，除非我明确要求。
```