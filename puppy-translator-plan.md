# 小狗语言翻译器微信小程序项目计划

## 1. 项目概述

开发一款运行在微信小程序中的模拟“小狗语言翻译器”。用户真实录制一段声音后，系统从预设趣味文案库中随机选择内容并显示为便签。随机文字与音频内容没有分析关系。用户可以左滑删除结果，或右滑保存音频、便签以及现场拍摄的小狗照片。

产品定位为轻松、有趣的“小狗语言翻译器”。界面不展示生成机制说明，也不展示准确率、医疗诊断或健康建议。

## 2. 产品目标

### 2.1 MVP 目标

- 用户可以开始、停止和取消录音。
- 录音完成后，系统可以随机生成一条趣味文案。
- 随机生成的趣味文字以便签形式显示。
- 左滑便签可以删除结果及相关临时文件。
- 右滑便签可以保存音频和随机生成的便签。
- 保存时可以拍摄或选择一张小狗照片。
- 用户可以在历史记录中查看便签、照片并播放音频。
- 上传或保存失败后可以重新尝试。
- 用户可以删除自己保存的数据。

### 2.2 暂不纳入 MVP 的功能

- 用户社区、动态和评论。
- 排行榜、积分和复杂会员体系。
- 多只小狗的精细身份识别。
- 兽医诊断或健康治疗建议。
- 实时连续监听和后台录音。

## 3. 核心用户流程

1. 用户进入录音页面，点击或长按录音按钮。
2. 首次使用时，小程序申请麦克风权限。
3. 用户结束录音后，小程序展示短暂的“翻译中”趣味动画。
4. 客户端从内置文案库中随机选择一条文字，不上传音频做内容分析。
5. 页面生成一张结果便签，包含：
   - 随机趣味表达，例如“今天可以多给我一块小饼干吗？”
   - 趣味分类，例如撒娇、玩耍、好奇、贪吃或想睡觉。
   - 录音时长和生成时间。
6. 用户左滑便签时删除结果；建议提供 3 秒撤销入口，避免误删。
7. 用户右滑便签时保存音频和便签，并询问是否拍照或从相册选择照片。
8. 用户进入历史记录页面，可以查看、播放或删除已保存记录。

## 4. 交互与状态设计

### 4.1 录音状态

```text
idle -> recording -> stopped -> generating -> pending -> saved
                              |             |
                              v             v
                           failed        failed
```

状态说明：

- `idle`：等待录音。
- `recording`：正在录音，显示时长和停止按钮。
- `generating`：展示短暂翻译动画并随机选择文案，禁止重复提交。
- `pending`：结果已生成但尚未保存。
- `saved`：音频和便签已正式保存。
- `failed`：录音、随机生成或保存失败，可以重试或删除。

### 4.2 左右滑动

- 使用 `touchstart`、`touchmove` 和 `touchend` 计算横向距离与速度。
- 横向移动明显大于纵向移动时，才识别为滑动操作。
- 设置最小触发距离，避免用户滚动页面时误触。
- 左滑显示删除操作，并执行数据库和云文件清理。
- 右滑显示保存操作，然后进入拍照或选图流程。
- 正在生成或保存的便签不允许重复滑动操作。

### 4.3 异常处理

- 用户拒绝麦克风权限时，展示进入微信设置的入口。
- 用户拒绝相机权限时，仍允许不带照片保存。
- 弱网或断网不影响本地随机文案生成；云端保存失败时保留本地待处理任务。
- 录音过短时提示重新录制；静音不影响随机文案生成。
- 页面使用轻松的趣味文案，不展示准确率、可信度或医疗判断。

## 5. 技术选型

### 5.1 小程序端

- 框架：微信原生小程序。
- 语言：TypeScript、WXML、WXSS、JSON。
- 录音：`wx.getRecorderManager()`。
- 拍摄与相册：`camera` 组件或 `wx.chooseMedia()`。
- 本地缓存：微信 Storage API，用于保存待上传和待重试任务。
- 网络通信：调用微信云函数或通过 HTTPS 请求后端接口。
- UI：优先编写轻量原生组件；需要通用控件时可选 WeUI Miniprogram。

选择原生小程序的原因是录音、相机、权限和云开发 API 可以直接使用，MVP 阶段不需要引入跨端框架的额外复杂度。

### 5.2 随机文案生成

- 运行位置：小程序客户端本地，不需要远程 AI 服务。
- 文案来源：版本化的 TypeScript/JSON 预设文案库。
- 随机方式：使用均匀随机或按趣味分类设置权重。
- 防重复：记录最近生成的文案 ID，尽量避免连续出现相同内容。
- 展示节奏：录音结束后播放约 1～2 秒“翻译中”动画，再展示结果。
- 结果字段：文案 ID、分类、显示文字、生成时间和生成器版本。

### 5.3 云端业务

- 运行时：Node.js + TypeScript。
- 平台：优先使用微信云开发 CloudBase。
- 数据库：CloudBase 云数据库。
- 文件：CloudBase 云存储。
- 身份识别：由云函数获取微信用户 `openid`，不信任客户端提交的用户身份。
- 定时清理：定时任务删除超期的临时音频和无效文件。

云端只负责保存用户主动右滑保留的录音、照片和便签，不做音频分析。随机文案在客户端生成，因此即使断网也能立即得到结果。

## 6. 推荐代码结构

```text
puppy-translator/
|-- miniprogram/
|   |-- app.ts
|   |-- app.json
|   |-- app.wxss
|   |-- pages/
|   |   |-- recorder/          # 录音、分析进度、结果便签
|   |   |-- history/           # 已保存记录列表
|   |   `-- detail/            # 音频播放、照片和详细结果
|   |-- components/
|   |   |-- record-button/
|   |   |-- result-note/
|   |   |-- swipe-item/
|   |   `-- audio-player/
|   |-- services/              # 云函数、上传和权限调用封装
|   |-- store/                 # 页面状态和待重试任务
|   |-- data/                  # 随机趣味文案库
|   |-- generators/            # 随机选择与防重复逻辑
|   |-- utils/
|   `-- types/
|-- cloudfunctions/
|   |-- createTranslation/
|   |-- saveTranslation/
|   |-- deleteTranslation/
|   |-- listTranslations/
|   `-- cleanupTemporaryFiles/
|-- packages/
|   `-- contracts/             # 前后端共享字段和状态定义
|-- tests/
|-- scripts/
|-- docs/
|-- package.json
|-- tsconfig.json
`-- project.config.json
```

## 7. 数据设计

### 7.1 `translations` 集合

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_id` | string | 记录 ID |
| `openid` | string | 数据所有者，仅服务端写入 |
| `audioFileId` | string | 云存储音频文件 ID |
| `photoFileId` | string/null | 可选照片文件 ID |
| `displayText` | string | 便签上显示的推测内容 |
| `messageId` | string | 随机文案 ID |
| `messageCategory` | string | 趣味文案分类 |
| `duration` | number | 录音时长，单位为毫秒 |
| `status` | string | `pending`、`saved`、`failed` 或 `deleted` |
| `generatorVersion` | string | 文案库或生成器版本 |
| `errorCode` | string/null | 失败时的错误码 |
| `createdAt` | date | 创建时间 |
| `savedAt` | date/null | 正式保存时间 |

### 7.2 文件生命周期

- 用户右滑确认保存后，将音频上传到临时目录。
- 右滑保存后，将记录标记为 `saved`，文件进入长期保存范围。
- 左滑删除时，同时删除音频、照片和数据库记录。
- 未保存的临时文件建议在 24 小时后自动清理。
- 数据库操作与文件删除需要支持重试，并记录清理失败日志。

## 8. 接口与云函数设计

### 8.1 `createTranslation`

- 校验调用用户和音频文件归属。
- 校验文件格式、大小和录音时长。
- 接收客户端已经生成的文案 ID。
- 服务端校验文案 ID 是否存在于当前允许的文案版本中，防止写入任意内容。
- 创建 `pending` 记录并保存文案、录音时长和生成器版本。

### 8.2 `saveTranslation`

- 校验记录所有者。
- 接收可选照片文件 ID。
- 将记录从 `pending` 更新为 `saved`。
- 防止重复提交造成多条记录。

### 8.3 `deleteTranslation`

- 校验记录所有者。
- 删除关联音频和照片。
- 删除记录或保留不含用户内容的审计状态。
- 返回可供客户端确认的清理结果。

### 8.4 `listTranslations`

- 仅查询当前用户已保存的数据。
- 使用分页，按创建时间倒序排列。
- 返回播放音频和加载照片所需的信息。

## 9. 随机文案实现计划

### 9.1 文案库

- 第一版准备 80～150 条简短趣味文案。
- 按撒娇、玩耍、好奇、贪吃、想睡觉和调皮等分类组织。
- 每条文案包含稳定 ID、分类、正文和启用状态。
- 避免医疗判断、恐吓、攻击性或容易误解为真实建议的内容。
- 文案长度要适合手机便签，建议不超过 40 个汉字。

### 9.2 AI 辅助生成文案库

AI 只在开发和内容维护阶段批量生成候选文案，不在小程序运行时调用，也不接收用户录音。

生成与导入流程：

1. 先定义允许的分类、文字风格、长度上限和禁止内容。
2. 让 AI 按固定 JSON 格式生成 200～300 条候选文案，候选数量应大于最终需要的数量。
3. 将原始候选保存到开发用草稿文件，不直接打包进小程序。
4. 运行校验脚本，检查 JSON 格式、重复 ID、重复正文、非法分类、空内容和长度超限。
5. 人工逐条审核，删除医疗判断、危险引导、冒犯内容、过度重复和不自然表达。
6. 从审核通过的候选中选择 80～150 条，分配稳定 ID 和文案库版本。
7. 将正式内容导出到小程序的只读 TypeScript/JSON 文案库。
8. 运行单元测试和界面预览，确认最长文案不会导致便签溢出。

候选文案数据格式：

```json
{
   "id": "food-001",
   "category": "贪吃",
   "text": "我觉得现在正好适合加一顿小零食。",
   "enabled": true
}
```

给 AI 的内容生成要求至少包括：

- 使用小狗第一人称，语气轻松、自然、有变化。
- 每条不超过 40 个汉字，不使用换行、表情符号和网络链接。
- 各分类数量尽量均衡，同一句式不要只替换少量词语重复生成。
- 不生成医疗、疾病、受伤、攻击、恐吓、歧视、成人或危险行为内容。
- 不输出准确率、可信度、真实情绪判断或健康建议。
- 只输出符合约定结构的 JSON 数组，不附加解释文字。

文案更新时重复执行“AI 生成候选、自动校验、人工审核、版本化导入”流程。未经人工审核的 AI 文案不得进入正式版本。

### 9.3 随机规则

- 录音成功结束后才允许生成文案。
- 文案选择与音频内容、音量和时长无关。
- 保存最近 5 条文案 ID，优先从未出现的候选中随机选择。
- 候选不足时清空历史并重新开始，保证逻辑不会卡死。
- 随机生成逻辑写成纯函数，便于测试候选过滤和边界情况。

### 9.4 趣味表现

- 录音结束后展示 1～2 秒固定时长的“正在翻译汪语”动画。
- 动画只用于娱乐节奏，不能声称正在做真实声音分析。
- 结果便签只展示趣味分类、正文、录音时长和生成时间。
- 可以按文案分类改变便签图标或辅助色，但不能暗示准确度。

## 10. 依赖工具

### 10.1 开发工具

- 微信开发者工具。
- Visual Studio Code。
- Node.js LTS 和 npm 或 pnpm。
- Git。

### 10.2 建议依赖

小程序和云函数：

- `typescript`
- `miniprogram-api-typings`
- `eslint`
- `prettier`
- `vitest` 或 `jest`
- `miniprogram-simulate`
- `miniprogram-ci`

具体版本在开始开发时锁定，并提交 lockfile，避免开发和发布环境依赖不一致。

## 11. 本地编译与运行

### 11.1 小程序与云函数

```powershell
npm install
npm run lint
npm run typecheck
npm test
```

随后在微信开发者工具中：

1. 导入项目目录。
2. 填写小程序 AppID。
3. 选择或创建云开发环境。
4. 上传并部署云函数及其依赖。
5. 使用模拟器检查布局，再使用真机预览测试录音和相机。

## 12. 测试计划

### 12.1 单元测试

- 录音状态机转换。
- 滑动方向、距离和阈值计算。
- 音频时长和文件大小校验。
- 随机文案候选过滤、防连续重复和空候选回退。
- 权限错误、网络错误和服务端错误转换。
- 临时文件生命周期和清理逻辑。

### 12.2 接口测试

- 未登录或伪造身份无法访问数据。
- 用户不能读取、修改或删除其他用户的记录。
- 重复保存不会生成重复数据。
- 删除记录后音频和照片同步删除。
- 客户端提交不存在的文案 ID 时服务端拒绝保存。

### 12.3 真机测试

- iOS 和 Android 微信客户端。
- 不同屏幕尺寸和系统字体设置。
- 拒绝、临时授权和取消麦克风或相机权限。
- 来电、锁屏、切后台等录音中断场景。
- 静音、背景噪声、人的声音、超短和超长录音；除超短录音外均可生成随机文案。
- Wi-Fi、移动网络、弱网、断网和网络切换。
- 快速重复点击、连续滑动和页面滚动时的误触。
- 多张便签和大量历史记录下的性能。

### 12.4 发布验收标准

- 用户可以在 3 个主要操作内完成录音并看到结果。
- 左滑可以删除结果和相关文件。
- 右滑可以保存音频、便签并关联可选照片。
- 历史记录可以稳定加载和播放音频。
- 上传或保存失败后可以重试。
- 用户可以删除自己的全部内容。
- 结果便签不显示可信度、准确率或医疗判断。
- 关键流程在目标 iOS 和 Android 设备上通过真机测试。

## 13. 隐私、安全与合规

- 在微信小程序隐私保护指引中说明麦克风、相机、相册、音频和照片的用途。
- 明确数据保存期限和删除方式，并说明录音不会用于内容分析或模型训练。
- 只在用户主动录音时采集音频，不进行后台监听。
- 文件上传和 API 调用必须使用 HTTPS。
- 所有读取、保存和删除操作在服务端校验 `openid`。
- 限制音频和图片的格式、大小、时长及请求频率。
- 日志不得保存完整音频、照片、`openid` 或其他不必要的个人信息。
- 为数据库和云存储配置最小权限。
- 提供隐私政策、用户协议和账户数据删除入口。
- 根据小程序主体、服务类目和服务器部署地区确认备案要求。
- 产品文案不宣传准确翻译，不提供医疗诊断或健康建议。

## 14. 发布流程

1. 注册微信小程序账号并完成主体认证。
2. 申请 AppID，选择合适的服务类目。
3. 创建生产云环境，配置数据库、云存储和安全规则。
4. 在微信公众平台配置服务器域名和隐私保护指引。
5. 使用微信开发者工具生成体验版，邀请内部和小范围用户测试。
6. 修复阻断问题并完成发布验收。
7. 使用微信开发者工具或 `miniprogram-ci` 上传代码。
8. 填写版本说明、测试账号和功能演示路径并提交审核。
9. 审核通过后先灰度发布，再逐步全量开放。
10. 发布后监控接口错误率、上传成功率和删除任务状态。

## 15. 开发阶段与时间估算

以下估算适用于一名开发者按顺序完成小程序和云开发功能：

| 阶段 | 时间 | 交付内容 |
| --- | --- | --- |
| 产品与设计 | 第 1 周 | 流程图、页面原型、视觉规范、数据和隐私方案 |
| 小程序 MVP | 第 2～3 周 | 真实录音、随机文案、便签、滑动、拍摄、历史记录 |
| 云端闭环 | 第 4 周 | 云函数、数据库、存储、鉴权、失败重试和文件清理 |
| 测试发布 | 第 5 周 | 真机回归、性能优化、合规材料、灰度发布 |

不需要算法工程、训练数据或模型部署。若先发布纯本地版本、不做云端同步，开发周期还可以进一步缩短。

## 16. 风险与应对

| 风险 | 应对方案 |
| --- | --- |
| 产品文案造成过度承诺 | 不宣传准确率、AI 识别或医疗诊断，保持轻松的趣味表达 |
| 文案重复导致新鲜感下降 | 增加文案数量，保存最近结果并进行防连续重复选择 |
| 随机文案出现不合适内容 | AI 候选经过人工审核后才进入预设库，小程序运行时不调用生成模型 |
| 上传等待过长 | 随机文案先在本地立即生成，音频仅在用户保存时上传 |
| 音频和照片带来隐私风险 | 最小化采集、设置临时文件过期、支持彻底删除 |
| 左右滑动容易误触 | 设置方向锁、距离阈值、操作提示和删除撤销 |
| 云端费用不可控 | 限制录音时长、文件大小和保存频率，监控存储与流量 |

## 17. 运营与迭代指标

第一版重点监控：

- 录音完成率。
- 上传成功率。
- 平均随机文案生成耗时。
- 结果保存率和删除率。
- 照片关联率。
- 生成失败率和保存重试成功率。
- 连续重复文案比例。
- 文件清理失败数量。
- 不同文案分类的保存率。

后续可以根据保存率和用户主动反馈扩充趣味文案库，但不收集“翻译准确度”，也不将用户音频用于训练。

## 18. 下一步执行清单

1. 确认产品名称和“随机趣味翻译”的定位。
2. 确认录音交互采用点击切换还是长按录音。
3. 绘制录音页、结果便签、拍照流程和历史页原型。
4. 注册小程序并创建开发用云环境。
5. 初始化 TypeScript 小程序和云函数目录。
6. 使用 AI 批量生成候选文案，经过自动校验和人工审核后导入第一版趣味文案库。
7. 完成真实录音、随机便签、滑动、拍照和保存的端到端 MVP。
8. 真机验证通过后配置云存储、隐私说明并提交体验版。