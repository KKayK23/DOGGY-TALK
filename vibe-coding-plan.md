# 小狗语言翻译器 Vibe Coding 分步计划

## 1. 使用方式

这份计划把项目拆成多轮可以独立运行、验证和回退的 Vibe Coding 任务。每轮只解决一个主要问题，完成验收后再进入下一轮，不要在一次对话中要求 AI 生成整个项目。

推荐每轮遵循以下节奏：

1. 把本轮提示词交给编程助手。
2. 让助手先阅读现有代码和上一轮结果，再开始修改。
3. 要求助手只改本轮范围内的文件，不顺手重构无关代码。
4. 在微信开发者工具和真机上执行本轮验收。
5. 修复本轮问题，确保编译和测试通过。
6. 使用 Git 提交当前可运行版本，再进入下一轮。

每轮通用提示词前缀：

```text
请先阅读 puppy-translator-plan.md、vibe-coding-plan.md、vibe-coding-rules.md 和当前代码。
严格遵守 vibe-coding-rules.md 中的产品不可变规则、安全规则和完成标准。
只实现本轮要求，不提前实现后续功能，不改动无关文件。
保持微信原生小程序、TypeScript 和现有代码风格。
完成后运行当前项目已有的类型检查、测试或构建，并说明修改文件、验证结果和遗留问题。
```

## 2. 阶段总览

| 阶段 | 轮次 | 最终成果 |
| --- | --- | --- |
| 基础框架 | 第 0～2 轮 | 工程可运行，录音页和模拟便签可交互 |
| 核心体验 | 第 3～6 轮 | 真录音、随机文案、滑动、照片和本地历史闭环 |
| 云端能力 | 第 7～10 轮 | 云存储、云函数、用户数据和清理机制 |
| 趣味内容 | 第 11～13 轮 | 文案库、防重复随机规则和翻译动画 |
| 产品完善 | 第 14～17 轮 | 异常恢复、测试、隐私合规和正式发布 |

## 3. 第 0 轮：确认开发环境与产品边界

### 本轮目标

在写业务代码前确认工具、账号、关键交互和产品表述，消除会影响后续结构的歧义。

### 实现内容

- 安装 Node.js LTS、微信开发者工具、Git 和 VS Code。
- 注册微信小程序并取得开发用 AppID；暂时也可使用测试号。
- 确认录音交互采用“点击开始，再次点击结束”。
- 确认产品使用“随机趣味翻译”，结果与录音内容无关。
- 确认单次录音时长范围，建议最短 1 秒、最长 15 秒。
- 建立 Git 仓库和 `.gitignore`，排除依赖、构建输出和本地私密配置。

### 暂不实现

- 页面业务代码。
- 云开发和随机文案业务。

### 验收标准

- 微信开发者工具可以登录并创建测试项目。
- 命令行可以运行 `node --version`、`npm --version` 和 `git --version`。
- 上述产品决定已写入项目文档。

### 本轮提示词

```text
请检查当前空项目和开发目标，建立最小 Git 忽略规则和环境说明文档。
不要生成业务页面。列出我仍需在微信开发者工具中手工完成的配置，并确保任何 AppID、密钥和本地配置都不会提交到 Git。
```

## 4. 第 1 轮：初始化最小微信小程序

### 本轮目标

生成能够在微信开发者工具中直接编译的原生 TypeScript 小程序骨架。

### 实现内容

- 创建 `miniprogram`、`cloudfunctions` 和基础配置文件。
- 创建 `app.ts`、`app.json`、`app.wxss`。
- 创建录音页 `pages/recorder` 和历史页 `pages/history`。
- 配置底部导航或简单页面跳转。
- 配置 TypeScript、ESLint、Prettier 和 npm scripts。
- 页面暂时只显示标题和占位内容。

### 暂不实现

- 真实录音。
- 云函数和数据库。
- 复杂视觉设计。

### 验收标准

- 微信开发者工具编译无错误。
- 录音页与历史页可以互相切换。
- `npm run typecheck` 和 `npm run lint` 可以执行。
- 项目中没有硬编码密钥。

### 本轮提示词

```text
初始化微信原生 TypeScript 小程序最小工程。创建录音页和历史页，只做可编译的页面骨架与导航。
同时配置 TypeScript、ESLint、Prettier 和 npm scripts。不要实现录音、随机文案或云开发。
```

## 5. 第 2 轮：搭建录音页静态界面

### 本轮目标

先完成不依赖录音 API 的视觉和组件框架，确定主要布局在不同屏幕上稳定。

### 实现内容

- 设计录音页主区域、状态文字、录音按钮和结果区域。
- 创建 `record-button` 和 `result-note` 基础组件。
- 使用模拟数据展示一张便签，包括趣味分类、随机文字、录音时长和时间。
- 加入录音、处理中、结果完成三种静态展示状态。
- 使用稳定尺寸约束，避免按钮和便签随文字跳动。
- 添加必要的无障碍标签和按钮反馈状态。

### 暂不实现

- 调用麦克风。
- 便签滑动手势。
- 保存和删除逻辑。

### 验收标准

- 常见手机宽度下没有文字溢出、遮挡或横向滚动。
- 状态切换不会造成明显布局跳动。
- 录音按钮是明确的主要操作，历史入口容易找到。
- 模拟便签数据通过 TypeScript 类型约束。

### 本轮提示词

```text
实现录音页静态 UI 和 record-button、result-note 组件。使用模拟数据展示 idle、recording、generating、pending 四种状态。
界面应适合微信小程序，保证小屏幕不溢出。不要接入真实录音、滑动、存储或云开发。
```

## 6. 第 3 轮：接入真实录音

### 本轮目标

使用微信录音 API 完成开始、计时、结束和取消录音。

### 实现内容

- 封装 `wx.getRecorderManager()`。
- 第一次录音前处理麦克风授权。
- 点击按钮开始录音，再次点击结束录音。
- 显示实时录音时长，达到 15 秒时自动停止。
- 处理 `onStart`、`onStop`、`onError` 和页面卸载。
- 保存本次临时音频路径和录音时长。
- 少于 1 秒的录音不进入随机文案生成流程。

### 暂不实现

- 上传音频。
- 随机文案生成。
- 录音波形动画。

### 验收标准

- 真机可以开始和停止录音。
- 连续点击不会创建多个录音任务或计时器。
- 页面离开后录音与计时器正确停止。
- 拒绝权限、录音过短和录音失败都有明确反馈。

### 本轮提示词

```text
在现有录音页接入 wx.getRecorderManager，实现点击开始、再次点击结束、最长 15 秒自动结束和最短 1 秒校验。
封装录音状态，处理授权拒绝、API 错误、重复点击和页面卸载。结束后只保存临时路径和时长，不上传、不生成文字。
```

## 7. 第 4 轮：建立状态机与随机文案闭环

### 本轮目标

让真实录音结束后从本地预设文案中随机选择一条，并稳定生成结果便签。

### 实现内容

- 定义录音任务状态：`idle`、`recording`、`generating`、`pending`、`saved`、`failed`。
- 建立统一 `TranslationRecord` TypeScript 类型。
- 建立第一版本地趣味文案数组，每条包含稳定 ID、分类和正文。
- 录音结束后展示 1～2 秒“正在翻译汪语”的固定趣味动画。
- 动画结束后从文案数组随机选择一条，与音频内容无关。
- 便签显示文案、趣味分类、录音时长和生成时间。
- 处理中禁止重复录音和重复提交。
- 处理文案库为空等失败场景，并提供重试和取消操作。

### 暂不实现

- 云存储和云函数。
- 左右滑动。

### 验收标准

- 完成录音后依次进入生成动画和结果状态。
- 任意时刻只有一个有效任务。
- 文案生成不依赖网络，断网时也能正常得到结果。
- 结果不展示可信度、准确率或医疗判断。
- 刷新或退出页面不会残留失控的定时器。

### 本轮提示词

```text
为录音流程建立明确状态机和 TranslationRecord 类型。录音结束后展示固定时长的趣味翻译动画，再从本地预设数组随机选择文字生成便签。
文字必须与音频内容无关，便签只展示趣味结果、分类、录音时长和生成时间。不要上传音频或接入云开发。为状态转换和随机选择增加单元测试。
```

## 8. 第 5 轮：实现便签左右滑动

### 本轮目标

完成便签左滑删除、右滑保存的核心手势，同时避免与纵向滚动冲突。

### 实现内容

- 创建独立 `swipe-item` 组件。
- 使用触摸起点、横纵距离和速度判断滑动方向。
- 横向距离明显大于纵向距离时才锁定横向手势。
- 左滑显示删除操作，右滑显示保存操作。
- 未达到阈值时自动回弹。
- 生成中或失败状态禁止保存。
- 左滑删除后提供 3 秒本地撤销。
- 右滑先只修改本地状态为已保存。

### 暂不实现

- 数据库保存和云文件删除。
- 保存时拍照。

### 验收标准

- 页面上下滚动时不会频繁误触左右操作。
- 快速和缓慢滑动都遵守统一阈值。
- 删除可以在 3 秒内撤销，超时后不可恢复。
- 同一条记录不会重复保存或删除。

### 本轮提示词

```text
实现可复用 swipe-item 组件，为 pending 便签提供左滑删除和右滑保存。
加入方向锁、距离阈值、回弹和操作中的防重复机制。删除提供 3 秒撤销，保存本轮只更新本地状态。为手势计算提取纯函数并测试边界值。
```

## 9. 第 6 轮：照片与本地历史记录

### 本轮目标

在没有云端依赖的情况下完成“录音、便签、照片、保存、历史查看”的本地产品闭环。

### 实现内容

- 右滑保存后弹出照片选择流程。
- 使用 `wx.chooseMedia()` 支持拍照或从相册选择。
- 允许跳过照片并只保存音频和便签。
- 使用 Storage API 保存记录元数据。
- 将临时音频和照片保存到可持久化的本地文件位置。
- 历史页按时间倒序显示记录。
- 创建详情页，展示照片、便签并播放本地音频。
- 支持从历史页删除本地记录和关联文件。

### 暂不实现

- 多设备同步。
- 云数据库和云存储。

### 验收标准

- 关闭并重新进入小程序后，本地历史记录仍存在。
- 有照片和无照片两种记录都能正常展示。
- 音频可以播放、暂停，并在离开详情页时停止。
- 删除后记录、音频和照片均被清理。

### 本轮提示词

```text
在右滑保存后接入 wx.chooseMedia，允许拍照、选图或跳过。使用微信 Storage API 和本地文件能力保存记录、音频与可选照片。
实现历史列表和详情页，支持音频播放及彻底删除。本轮只做单机本地数据，不接入云开发。
```

## 10. 第 7 轮：初始化 CloudBase 云环境

### 本轮目标

建立云开发基础设施，但不立即替换已稳定的本地流程。

### 实现内容

- 在 `app.ts` 初始化指定 CloudBase 环境。
- 建立开发和生产环境配置方式，避免环境 ID 散落在业务代码中。
- 创建 `translations` 集合及索引说明。
- 创建最小 `login` 或 `getUserContext` 云函数，验证 `openid` 获取。
- 建立统一云函数调用封装和错误类型。
- 编写云环境部署说明。

### 暂不实现

- 云端保存业务记录。
- 上传音频和照片。
- 正式云端保存业务。

### 验收标准

- 真机可以成功调用测试云函数。
- 客户端不能自行指定或覆盖 `openid`。
- 开发与生产配置清晰分离。
- 云函数错误在客户端转换为可理解的业务错误。

### 本轮提示词

```text
在现有项目中初始化微信 CloudBase，增加开发/生产环境配置和统一云函数调用封装。
创建最小用户上下文云函数，由服务端读取 openid。不要改动当前本地保存流程，不要上传媒体或实现业务 CRUD。
```

## 11. 第 8 轮：上传音频和照片到云存储

### 本轮目标

把用户右滑确认保存的媒体文件从本地升级为安全、可重试的云存储上传。

### 实现内容

- 设计用户隔离的云存储路径，不在界面暴露 `openid`。
- 用户右滑保存后才上传音频到临时目录。
- 同一保存流程中上传可选照片。
- 展示上传进度并阻止重复上传。
- 校验文件扩展名、大小、录音时长和媒体类型。
- 上传失败后保留本地路径并支持重试。
- 记录本地任务与云文件 ID 的映射。

### 暂不实现

- 正式数据库 CRUD。
- 云端记录 CRUD。

### 验收标准

- 音频和照片可在云存储中找到，并能通过受控接口访问。
- 断网失败后重新联网可以重试。
- 重复点击不会产生多个同内容文件。
- 取消或删除未保存记录后，临时云文件进入待清理状态。

### 本轮提示词

```text
增加 CloudBase 音频和可选照片上传服务。上传过程要有进度、防重复、格式与大小校验，并保存本地任务和 fileID 的映射。
失败时保留可重试任务。不要实现数据库 CRUD，且不要信任客户端提供用户身份。未右滑保存的录音不应上传。
```

## 12. 第 9 轮：实现云端记录 CRUD

### 本轮目标

用云数据库替代本地记录作为主要数据源，实现用户隔离和多设备同步。

### 实现内容

- 实现 `createTranslation`、`saveTranslation`、`listTranslations` 和 `deleteTranslation` 云函数。
- 所有云函数从服务端上下文取得 `openid`。
- 保存操作设计幂等键，避免重复记录。
- 历史列表支持按时间倒序分页。
- 删除操作同时处理数据库、音频和照片。
- 本地 Storage 仅保留缓存和待同步任务。
- 对云函数输入进行结构化校验。

### 暂不实现

- 自动过期清理任务。

### 验收标准

- 同一用户重新安装或换设备后仍能读取云记录。
- 用户不能读取、修改或删除其他用户数据。
- 连续触发两次保存只生成一条记录。
- 删除记录后数据库和媒体文件最终都被清理。
- 列表分页没有重复和遗漏。

### 本轮提示词

```text
实现 translations 的云端 CRUD 云函数，并把历史页数据源迁移到云端。
所有身份必须从云函数上下文获取；加入输入校验、分页、幂等保存和媒体清理。Storage 只保留缓存和失败重试任务。为云函数核心逻辑增加测试。
```

## 13. 第 10 轮：临时文件清理与数据一致性

### 本轮目标

补齐云文件生命周期，防止上传成功但业务失败后持续产生无主文件和费用。

### 实现内容

- 为临时文件记录创建时间、所有者和关联任务 ID。
- 创建 `cleanupTemporaryFiles` 定时云函数。
- 自动清理超过 24 小时且未保存的音频和照片。
- 删除流程采用可重试的状态，而不是假设数据库和文件同时成功。
- 记录不含敏感内容的清理错误日志。
- 增加手动重试清理入口或运维脚本。

### 验收标准

- 测试用过期文件可被定时任务清理。
- 已保存记录的媒体不会被误删。
- 部分删除失败时，任务会保留并能再次执行。
- 日志中不包含完整音频、照片或敏感用户数据。

### 本轮提示词

```text
实现临时媒体生命周期和 cleanupTemporaryFiles 定时云函数，清理超过 24 小时且未保存的文件。
删除与清理必须可重试并避免误删正式记录。增加测试覆盖过期、未过期、已保存和部分失败四种情况。
```

## 14. 第 11 轮：用 AI 生成并审核趣味文案库

### 本轮目标

使用 AI 批量生成候选内容，再通过自动校验和人工审核建立可维护的正式文案库。

### 实现内容

- 先定义允许分类、文字风格、长度上限和禁止内容。
- 使用 AI 按固定 JSON 格式生成 200～300 条候选文案。
- 候选文案只用于开发期草稿，不直接打包进小程序。
- 编写校验脚本，检查 JSON 格式、重复 ID、重复正文、非法分类、空内容和长度超限。
- 人工逐条审核候选，最终保留 80～150 条趣味文案。
- 文案按撒娇、玩耍、好奇、贪吃、想睡觉和调皮等分类。
- 每条文案包含稳定 ID、分类、正文和启用状态。
- 增加文案库版本，保存记录时写入 `generatorVersion`。
- 校验 ID 唯一、正文非空、分类合法和长度不超过 40 个汉字。
- 移除可能被理解为医疗、危险或真实行为判断的内容。
- 将文案库放在独立 TypeScript/JSON 文件中，业务组件不直接硬编码文字。
- 保存一份生成要求模板，后续扩充文案时复用相同审核流程。

### 暂不实现

- 用户在线编辑或发布文案。
- 小程序运行时调用 AI 或把用户录音发送给模型。

### 验收标准

- 文案检查脚本或测试可以发现重复 ID、空内容和超长内容。
- 所有启用文案都属于允许分类。
- 文案库不包含可信度、诊断或真实情绪判断字段。
- 正式文案均有人工审核记录，原始候选不会被小程序引用。
- 新增文案不需要修改录音页组件代码。

### 本轮提示词

```text
把当前占位文字扩充为独立、版本化的正式趣味文案库。先定义分类、风格、长度和禁止内容，再使用 AI 按固定 JSON 格式生成 200～300 条候选。
候选使用小狗第一人称，每条不超过 40 个汉字，分类数量尽量均衡，不使用表情符号、链接，不包含医疗、危险、攻击、成人内容、准确率或健康建议。
编写校验脚本检查 JSON 格式、ID 和正文重复、空内容、非法分类及长度。候选保存在开发草稿中，不直接打包；等待人工审核后，只把选出的 80～150 条导入正式 TypeScript/JSON 文案库。
每条正式内容包含稳定 ID、分类、正文和启用状态，并记录 generatorVersion。小程序运行时不得调用 AI，也不得把用户录音发送给模型。
```

## 15. 第 12 轮：完善随机生成与防重复规则

### 本轮目标

让随机文案选择在保持简单的前提下更有变化，并避免连续出现同一句话。

### 实现内容

- 将随机选择提取为无 UI 依赖的纯函数。
- 从所有启用文案中选择，不读取或分析音频内容。
- 在 Storage 中记录最近 5 条文案 ID，并优先排除它们。
- 候选不足时安全重置最近记录，不允许出现无限循环。
- 可为不同趣味分类设置简单权重，但默认保持均匀随机。
- 支持注入随机数函数，使单元测试结果可重复。
- 服务端保存时校验 `messageId` 和 `generatorVersion` 是否属于允许文案库。

### 暂不实现

- 根据音量、频率、时长或音频摘要选择文案。
- 从网络请求随机文案。

### 验收标准

- 连续生成多次时不会立即重复同一文案。
- 关闭并重新打开小程序后，最近记录仍能用于防重复。
- 文案库只有 1 条、候选为空或 Storage 损坏时仍能安全返回或明确失败。
- 飞行模式下可以完成真录音和随机便签生成。
- 单元测试可通过注入固定随机数覆盖边界情况。

### 本轮提示词

```text
完善本地随机文案生成器：排除最近 5 条结果，候选不足时安全重置，并支持注入随机数函数以便测试。
生成器不得读取音频内容，断网时必须可用。服务端保存记录时只校验 messageId 和 generatorVersion，不能接收客户端任意文字作为可信文案。
```

## 16. 第 13 轮：完善趣味翻译动画与结果呈现

### 本轮目标

让“翻译中”过程更有趣，并让结果出现得自然、清晰。

### 实现内容

- 录音结束后展示 1～2 秒固定时长的趣味翻译动画。
- 动画不等待网络，也不读取音频特征。
- 动画结束后以轻量过渡展示便签，避免页面突然跳动。
- 便签只展示趣味分类、正文、录音时长和生成时间。
- 首次使用时只在申请权限前说明麦克风用于录音。
- 动画期间禁止重复触发生成，退出页面时清理计时器。
- 为减少机械感，可以随机切换少量加载文案，但不得暗示真实分析步骤。

### 暂不实现

- 使用“频谱分析”“AI 识别中”或“准确率”等误导性文字。
- 因动画而延迟音频保存或阻塞页面离开。

### 验收标准

- 断网时动画和随机结果正常工作。
- 快速重复点击不会生成多张便签。
- 页面离开后没有残留动画或计时器。
- 动画结束后便签出现流畅，不造成布局跳动或内容遮挡。
- 界面不出现可信度、准确率、真实情绪或医疗暗示。

### 本轮提示词

```text
完善录音后的趣味翻译动画和结果呈现。动画固定 1～2 秒、本地运行、不读取音频特征，退出页面时清理。
便签只展示趣味分类、正文、录音时长和生成时间，移除准确率、可信度或医疗判断文案。
```

## 17. 第 14 轮：离线、弱网与任务恢复

### 本轮目标

让核心流程在网络波动、切后台和进程重启后可以恢复，而不是丢失用户录音。

### 实现内容

- 定义本地待处理任务队列。
- 保存本地文件路径、云文件 ID、任务阶段、重试次数和更新时间。
- 进入小程序时检查未完成任务。
- 联网后允许用户手动重试，必要时自动恢复一次。
- 使用幂等键避免恢复任务重复保存。
- 区分可重试错误和永久错误。
- 超过保留期限的失败任务提示用户删除或重录。

### 暂不实现

- 无限后台重试。
- 未经用户确认消耗大量移动数据。

### 验收标准

- 上传中断后重新进入小程序仍能看到待处理任务。
- 恢复任务不会生成重复记录或重复文件。
- 永久失败有明确原因和删除入口。
- 网络恢复后核心流程可以继续完成。

### 本轮提示词

```text
实现本地持久化任务队列，恢复上传和保存阶段的未完成任务。
任务必须包含幂等键、重试次数和错误分类；进入小程序时检测任务并让用户重试。不要做无限自动重试，避免重复云记录和重复媒体。
```

## 18. 第 15 轮：完善体验与边界状态

### 本轮目标

处理正常演示之外的真实使用细节，使应用达到内测质量。

### 实现内容

- 完善空列表、加载、失败、无网络和无权限页面状态。
- 增加录音音量或简单波形反馈，但不影响性能。
- 便签长文本使用合理换行和最大高度。
- 历史记录分页加载，并处理快速滚动。
- 照片加载失败显示占位状态。
- 音频播放支持播放、暂停、进度和离页停止。
- 删除和保存操作提供明确但不过度打扰的反馈。
- 统一颜色、字号、间距、图标和触控区域。

### 暂不实现

- 社区、会员或分享裂变。
- 与核心流程无关的装饰性动画。

### 验收标准

- 不同长度文案和系统字体下没有遮挡或溢出。
- 每个异步状态都有明确反馈，不出现无响应按钮。
- 低端设备上录音动画和列表滚动保持可用。
- iOS 和 Android 真机核心流程表现一致。

### 本轮提示词

```text
对现有小程序做内测前的体验完善，集中处理空、加载、失败、离线、无权限、长文本、图片失败和音频播放状态。
统一视觉变量与触控区域，保持现有产品风格。不要加入社区、会员、营销页面或无关动画。
```

## 19. 第 16 轮：补齐自动化测试与发布检查

### 本轮目标

把此前每轮的局部验证整理成可重复执行的质量检查。

### 实现内容

- 单元测试覆盖状态机、手势计算、结果映射和错误分类。
- 云函数测试覆盖身份隔离、输入校验、幂等和删除。
- 随机生成器测试覆盖防重复、空候选、单条文案和 Storage 损坏。
- 编写关键流程的手工真机测试清单。
- 统一 `lint`、`typecheck`、`test` 和构建脚本。
- 配置 `miniprogram-ci` 上传脚本，敏感参数使用环境变量。
- 生成发布前检查清单和版本说明模板。

### 验收标准

- 一条本地命令可以运行主要静态检查和测试。
- 测试失败时脚本返回非零退出码。
- 发布脚本不包含上传私钥或 AppID 私密配置。
- iOS 和 Android 真机清单完成一次全流程回归。

### 本轮提示词

```text
整理项目质量检查：补齐状态机、手势、随机生成器、错误映射、云函数权限与幂等的自动化测试。
统一 lint、typecheck、test 脚本，并配置不含密钥的 miniprogram-ci 上传脚本和真机回归清单。不要为了覆盖率重构无关业务。
```

## 20. 第 17 轮：隐私合规、灰度与正式发布

### 本轮目标

完成用户数据说明、安全复核、体验版验证和微信审核发布。

### 实现内容

- 增加隐私政策和用户协议入口。
- 清楚说明麦克风、相机、相册、音频和照片用途。
- 提供删除单条记录和清除全部用户内容的入口。
- 检查日志、错误监控和分析指标不包含不必要的敏感数据。
- 配置生产域名、CloudBase 环境、安全规则和服务限额。
- 上传体验版，邀请小范围用户完成真实场景测试。
- 修复阻断问题后提交微信审核。
- 先灰度发布，并监控错误率、上传成功率和文件清理状态。

### 验收标准

- 用户首次使用相关能力前可以了解数据用途。
- 用户可以删除自己的全部音频、照片和便签。
- 生产环境不存在测试密钥、调试接口或宽松数据库规则。
- 体验版通过完整真机回归。
- 审核材料使用“随机趣味翻译”，明确文字与录音内容无关，不宣传准确翻译或医疗诊断。

### 本轮提示词

```text
进行发布前隐私、安全和生产配置复核。补齐隐私政策、用户协议与清除全部数据功能。
检查云权限、日志脱敏、生产配置和服务限额，生成体验版测试与微信审核清单。不要在代码或文档中写入真实密钥。
```

## 21. 每轮结束时的固定检查

每轮完成后让编程助手回答以下问题：

1. 本轮修改了哪些文件，各自为什么修改？
2. 哪些需求已经完成，哪些明确留到后续轮次？
3. 执行了哪些编译、类型检查和测试，结果是什么？
4. 需要我在微信开发者工具或云平台手工完成什么？
5. 是否新增了环境变量、权限、数据库字段或云资源？
6. 是否存在未处理的临时文件、调试代码、模拟数据或硬编码配置？
7. 当前版本是否适合提交 Git；建议的提交信息是什么？

建议的检查提示词：

```text
请对照本轮验收标准做一次收尾检查。运行可用的类型检查、测试和构建，修复本轮引入的问题。
最后列出修改文件、验证结果、需要手工完成的配置、仍保留的 mock/临时代码，以及建议的 Git 提交信息。
```

## 22. Vibe Coding 过程中的约束

- 一轮只实现一个主目标，避免同时改小程序、云函数和多个交互层面。
- AI 生成代码后必须实际编译和真机验证，不能只看代码是否合理。
- 不把 AppSecret、云密钥、上传私钥或生产凭据发给编程助手，也不写入仓库。
- 不允许客户端传入 `openid` 作为可信身份，用户归属必须由云函数上下文确定。
- 不让 AI 猜测微信 API；遇到版本差异时以当前微信官方文档和类型定义为准。
- 不为了“代码更漂亮”在功能轮次中大规模重构稳定模块。
- 每完成一轮建立 Git 提交，发现回归时可以快速定位和回退。
- 开发期可以使用 AI 批量生成候选文案，但所有正式文案必须经过人工审核后进入本地预设库。
- 小程序运行时不调用文本生成模型，也不把用户录音发送给模型。
- 随机生成器不得读取音频内容、音量、频率或其他音频特征。
- 用户音频只用于播放和用户主动保存，不用于分析或训练。

## 23. 推荐里程碑

### 里程碑 A：可演示原型

完成第 1～5 轮。用户可以真实录音、等待趣味动画、看到随机便签，并执行左右滑动。此时适合验证交互和视觉方向。

### 里程碑 B：本地 MVP

完成第 6 轮。用户可以关联照片并在本机查看历史记录。此时即使没有云服务，也可以进行第一轮用户体验测试。

### 里程碑 C：云端 MVP

完成第 7～12 轮。媒体和记录通过真实云端链路保存，随机文案保持本地生成，可以邀请少量体验用户测试稳定性。

### 里程碑 D：内测版

完成第 13～15 轮。趣味动画、结果呈现、弱网恢复和主要边界状态已经完善，可以开始小范围内测。

### 里程碑 E：发布版

完成第 16～17 轮。测试、隐私、安全、审核材料和生产监控全部准备完成，可以灰度发布。