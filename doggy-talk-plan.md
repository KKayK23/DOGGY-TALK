# DOGGY TALK HTML 版專案計畫

## 1. 專案定位

DOGGY TALK 是一個以手機操作為主的趣味小狗語言翻譯網頁。使用者錄製一小段聲音後，網頁會從本機預設文案庫中隨機選出一句小狗口吻的文字，並以便簽形式呈現。

翻譯結果純屬娛樂，與錄音內容、音量、頻率及其他聲音特徵無關。網頁不進行 AI 分析、情緒辨識、醫療判斷或健康建議。

目前只開發 HTML 網頁版，主要實作檔案為 `doggy-talk.html`。不開發微信小程式、原生 App 或後端服務。

## 2. HTML 版目標

- 使用單一 HTML 檔案完成介面、樣式與互動邏輯。
- 在手機與桌面瀏覽器中提供穩定的響應式版面。
- 使用瀏覽器麥克風錄製 1 至 15 秒的聲音。
- 錄音完成後播放約 1 至 2 秒的趣味翻譯動畫。
- 從本機文案庫隨機產生便簽，並避免短時間內重複。
- 支援左滑刪除、右滑儲存，滑鼠與觸控操作皆可使用。
- 儲存時可選擇拍照、上傳圖片或不附照片。
- 在歷史紀錄中查看便簽、照片、時間及錄音長度。
- 支援播放音訊、刪除單筆紀錄及清除全部紀錄。
- 所有使用者內容只保存在目前瀏覽器的本機儲存空間。

## 3. 不納入目前版本

- 微信小程式、CloudBase、雲函數及微信登入。
- 帳號系統、跨裝置同步與多人共享。
- 伺服器、資料庫、雲端音訊或照片儲存。
- 真實的動物聲音辨識、AI 推論或模型訓練。
- 社群、留言、排行榜、會員與付費功能。
- 醫療診斷、健康監測或行為判斷。

## 4. 核心使用流程

1. 使用者開啟 `doggy-talk.html`，第一屏直接看到錄音主操作。
2. 使用者點擊錄音按鈕，瀏覽器在需要時要求麥克風權限。
3. 再次點擊可停止錄音；達到 15 秒時自動停止。
4. 少於 1 秒的錄音不產生結果，介面提示重新錄製。
5. 錄音完成後顯示固定時長的「正在翻譯汪語」動畫。
6. 網頁從本機文案庫隨機選出一句內容並顯示便簽。
7. 使用者可以左滑刪除便簽，並在 3 秒內復原。
8. 使用者可以右滑儲存便簽，選擇附加照片或直接略過。
9. 使用者可以開啟歷史紀錄，播放音訊或刪除已存內容。

若瀏覽器不支援錄音 API，仍可透過「快速試玩」體驗隨機便簽流程，但不建立真實錄音。

## 5. 介面與狀態

核心狀態如下：

```text
idle -> recording -> stopping -> generating -> pending -> saving -> saved
  ^                       |            |          |
  |                       v            v          v
  +--------------------- failed      deleted    failed
```

- `idle`：等待使用者開始錄音。
- `recording`：錄音中，顯示時間、停止按鈕及動態回饋。
- `stopping`：等待瀏覽器完成音訊封裝，禁止重複操作。
- `generating`：顯示本機趣味動畫並抽取隨機文案。
- `pending`：便簽已產生，等待刪除或儲存。
- `saving`：等待照片選擇與本機持久化完成。
- `saved`：紀錄已寫入本機儲存空間。
- `failed`：權限、錄音、儲存或讀取失敗，可回到可操作狀態。

介面視覺以 Figma 設計稿為準：

- 設計來源：`https://www.figma.com/design/q9Jvp6pJ5txL9WpFfqNmRf/ui-design`
- 本機素材位於 `Doggy talk png/`。
- 使用暖色紙張感、手繪小狗元素與清楚的錄音主操作。
- 所有文字在窄螢幕與系統大字體下不得溢出或互相遮擋。
- 動畫需遵守 `prefers-reduced-motion`。

## 6. 技術方案

### 6.1 檔案與語言

> **2026-09-16 更新**：程式規模已達到本節當初預留的拆分門檻，已完成工程化重構。
> 單一檔案版本封存於 `doggy-talk.html`（不再維護，僅作行為對照）。
> 現行架構為 Vite + ES 模組，詳見 `README.md`〈專案結構〉。

- 入口：`index.html`（純結構）+ `src/main.js`（模組入口）。
- 結構：語意化 HTML。
- 樣式：原生 CSS、CSS 自訂屬性與媒體查詢；拆分為 `src/styles/base/`（基礎）與 `src/styles/screens/`（Figma 對齊覆寫）兩層，cascade 順序見 README。
- 邏輯：原生 JavaScript（ES 模組），不使用 React、Vue 等框架；以 Vite 作為開發伺服器與建置工具。
- 圖示：Lucide Icons（npm 套件，僅引入實際用到的圖示）。
- 字體：Google Fonts；若要完整離線使用，後續需改成本機字體或系統備援字體。
- 素材：PNG 置於 `src/assets/`，由 Vite 指紋化輸出，不再以 base64 內嵌。

### 6.2 瀏覽器能力

- 麥克風權限：`navigator.mediaDevices.getUserMedia()`。
- 錄音：`MediaRecorder`。
- 照片選擇或拍攝：`<input type="file" accept="image/*" capture="environment">`。
- 手勢：Pointer Events，同時支援觸控與滑鼠。
- 紀錄中繼資料：`localStorage`。
- 音訊與照片二進位資料：IndexedDB。
- 音訊播放：`Audio` 與 `URL.createObjectURL()`。

麥克風通常只允許在 HTTPS 或 `localhost` 安全環境中使用。直接以 `file://` 開啟時，部分瀏覽器可能拒絕錄音，因此正式測試應使用本機 HTTP server。

### 6.3 隨機文案

- 文案內嵌於 HTML 的 JavaScript 資料中，不依賴網路請求。
- 每條文案包含穩定 ID、分類與正文。
- 最近 5 條文案 ID 寫入 `localStorage`，抽取時優先排除。
- 候選用完時回到完整文案庫，不允許無限迴圈。
- 文案不超過 40 個漢字，不包含醫療、危險、攻擊、成人或真實情緒判斷。
- 文案抽取不得讀取音訊 Blob、長度以外的資訊或任何音訊特徵。

## 7. 本機資料設計

### 7.1 紀錄中繼資料

```js
{
  id: "note-...",
  messageId: "cute-001",
  category: "撒嬌",
  text: "今天可以再多抱我一下嗎？",
  duration: 3200,
  createdAt: 1788364800000,
  hasAudio: true,
  hasPhoto: false
}
```

- 中繼資料陣列儲存在 `localStorage`。
- 錄音與照片 Blob 以紀錄 ID 為索引儲存在 IndexedDB。
- 歷史紀錄按 `createdAt` 由新到舊顯示。
- 刪除單筆紀錄時，必須同時刪除中繼資料、錄音與照片。
- 清除全部時，必須清理兩種儲存空間，不留下無主媒體。
- 若儲存空間不足，需顯示錯誤並保留目前便簽，避免假裝儲存成功。

瀏覽器資料可能因使用者清除網站資料、無痕模式結束或系統空間政策而消失，因此本機歷史不能視為永久備份。

## 8. 手勢規則

- 只有 `pending` 便簽可以觸發滑動操作。
- 移動超過 8 px 後才判定方向。
- 橫向距離至少為縱向距離的 1.2 倍時才鎖定水平手勢。
- 慢速滑動達便簽寬度 28% 時觸發。
- 快速滑動需同時達到 56 px 與 0.5 px/ms。
- 左滑只刪除，右滑只儲存；未達門檻時便簽回彈。
- 一次手勢最多執行一次操作，儲存或刪除期間禁止重複觸發。
- 左滑刪除後提供 3 秒復原操作。
- 需保留可由按鈕或鍵盤完成的替代操作，避免功能只依賴手勢。

## 9. 隱私與安全

- 只在使用者主動點擊錄音後要求麥克風權限。
- 照片為選填；不提供照片仍可儲存音訊與便簽。
- 錄音、照片及便簽不傳送到伺服器或 AI 服務。
- 不進行背景錄音或持續監聽。
- 介面需清楚說明資料只保留在目前裝置的瀏覽器中。
- 使用者可以刪除單筆紀錄及清除全部內容。
- 外部字體、圖示和示範圖片 CDN 仍會產生一般資源請求；正式離線版需將這些資源改為本機檔案。

## 10. 錯誤與邊界處理

- 麥克風權限被拒絕：顯示清楚提示，保留快速試玩入口。
- 不支援 `MediaRecorder`：停用真實錄音並提供試玩模式。
- 錄音少於 1 秒：不產生便簽，回到待錄音狀態。
- 錄音達到 15 秒：自動停止並進入產生流程。
- 錄音裝置中斷：釋放媒體軌與計時器，允許重新嘗試。
- `localStorage` 資料損壞：回退為空陣列，不讓頁面崩潰。
- IndexedDB 讀寫失敗：顯示錯誤，不宣告儲存成功。
- 圖片取消選擇或載入失敗：仍可無照片儲存。
- 同一時間只播放一筆歷史錄音；關閉歷史視窗時停止播放。

## 11. 測試計畫

### 11.1 桌面瀏覽器

- Chrome、Edge、Firefox 與 Safari 最新穩定版。
- 允許及拒絕麥克風權限。
- 開始、停止、少於 1 秒與 15 秒自動停止。
- 滑鼠拖曳的左右滑動、門檻回彈與刪除復原。
- 有照片、無照片、音訊播放、單筆刪除與清除全部。
- 重新整理及重新開啟頁面後，本機歷史仍可讀取。

### 11.2 手機瀏覽器

- iOS Safari 與 Android Chrome 真機。
- 不同螢幕寬度、安全區域及系統字體大小。
- 觸控滑動不應干擾頁面垂直捲動。
- 錄音期間切換分頁、鎖定螢幕或中斷麥克風。
- 相機拍攝、相簿選圖、取消及權限拒絕。
- 音訊可播放、暫停，關閉歷史紀錄後停止。

### 11.3 無障礙與品質

- 鍵盤可到達所有按鈕及對話框操作。
- 焦點樣式、按鈕名稱與 `aria-live` 狀態可理解。
- 僅使用鍵盤時也能儲存或刪除目前便簽。
- `prefers-reduced-motion` 下不播放持續動畫。
- 窄螢幕、長文案與放大 200% 時不出現文字遮擋。
- 瀏覽器主控台沒有未處理例外。

## 12. 發布方式

目前版本不需要編譯，可部署到任何靜態網站服務，例如 GitHub Pages、Cloudflare Pages、Netlify 或一般 HTTPS 主機。

發布要求：

- 必須使用 HTTPS，確保瀏覽器允許麥克風存取。
- 不提交測試錄音、真實照片、密鑰或私人資料。
- 發布前檢查外部 CDN 是否可用；需要離線或更高隱私時改用本機資源。
- 使用手機與桌面實際走完錄音、產生、儲存、播放及刪除流程。

## 13. 開發優先順序

1. 依照 Figma 設計稿修正 HTML 版的版面、圖像與所有狀態。
2. 完成並驗證真實錄音、15 秒限制與權限錯誤處理。
3. 完善便簽滑動、按鈕替代操作及刪除復原。
4. 驗證 IndexedDB 的音訊與照片儲存、播放及完整刪除。
5. 補齊行動裝置、鍵盤、縮放及 reduced motion 測試。
6. 將需要的遠端字體、圖示與圖片本機化，再發布 HTTPS 靜態網站。

## 14. HTML 版完成標準

- 第一屏可直接開始錄音，不需要登入或進入其他頁面。
- 錄音、趣味動畫、隨機便簽、刪除及儲存流程可完整使用。
- 隨機結果不依賴音訊內容或網路。
- 歷史紀錄可以顯示便簽與照片，並播放已保存錄音。
- 重新整理頁面後，本機紀錄仍存在。
- 使用者可以刪除單筆及全部本機資料。
- iOS Safari、Android Chrome 及主流桌面瀏覽器完成核心流程測試。
- 手機與桌面沒有內容溢出、控制項重疊或不可操作狀態。
- 頁面不宣稱真實翻譯、AI 辨識、準確率或醫療用途。
- 專案執行不需要微信開發者工具、CloudBase、Node.js 或建置流程。
